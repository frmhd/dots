// Startup applications
// Long-running session services are managed by systemd user units.

spawn-at-startup "wl-paste" "--watch" "cliphist" "store"
spawn-sh-at-startup "niri-float-sticky -title \"Picture in picture\""
